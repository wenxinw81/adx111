name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pyinstaller

      - name: Build direct executable
        shell: pwsh
        run: |
          $env:ADX_PACKAGE_VARIANT = "direct"
          pyinstaller --clean --noconfirm packaging/ADXReportAgent.spec

      - name: Package direct artifact
        shell: pwsh
        run: |
          Compress-Archive -Path dist/ADXReportAgent-Direct -DestinationPath dist/ADXReportAgent-Direct-windows.zip -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ADXReportAgent-Direct-windows
          path: dist/ADXReportAgent-Direct-windows.zip
