# 默认直连 Doris。公司内同事通常不需要修改这些连接信息。
DORIS_HOST=192.168.100.23
DORIS_PORT=29030
DORIS_DATABASE=ads
DORIS_USER=WishFox
DORIS_PASSWORD=WishFox#2026

# 默认直连，不走 SSH
ADX_SSH_ENABLED=false

# SSH 备用：如果所在电脑不能直连 Doris，再把 ADX_SSH_ENABLED 改成 true
ADX_SSH_USER=noel
ADX_SSH_HOST=192.168.110.139
ADX_SSH_PASSWORD=0816
ADX_SSH_LOCAL_PORT=19030

# Web 服务端口，可按需修改
ADX_WEB_HOST=0.0.0.0
ADX_WEB_PORT=8787

# 默认已集成 data/app_mapping.csv；如需换新表再打开这一行
# APP_MAPPING_CSV=data/app_mapping.csv
